export default function Navbar() {
  return (
    <nav className="navbar">
      <div className="logo">TAS NADEO</div>

      <input
        className="search"
        type="text"
        placeholder="Search tracks..."
      />
    </nav>
  );
}